#include <bits/stdc++.h>
#include "../grid-dp/tools.h"
#include "../../add/add.h"
using namespace std;

int lps(const string &str) {
    int n = str.size();
    int maxLength = 0;

    

    return maxLength;
}

int main() {
    string str = "bbbdb";

    cout << lps(str) << "\n";

    return 0;
}